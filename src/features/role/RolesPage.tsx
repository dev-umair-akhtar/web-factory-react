import { Box, Typography } from "@mui/material";

const Role = () => {
    return (
        <Box>
            <Typography variant="h4">Role</Typography>
        </Box>
    );
};

export default Role;
