import { Box, Table, TableBody, TableContainer } from "@mui/material";

const DataList = () => {
    return (
        <Box>
            <TableContainer>
                <Table>
                    <TableBody></TableBody>
                </Table>
            </TableContainer>
        </Box>
    );
};

export default DataList;
